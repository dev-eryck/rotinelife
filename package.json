{
  "name": "rotinelife",
  "version": "1.0.0",
  "description": "RotineLife - Organizador Financeiro Pessoal",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "start": "cd frontend && npm start"
  },
  "keywords": [
    "finanças",
    "organizador",
    "financeiro",
    "react",
    "nodejs"
  ],
  "author": "ErycK",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}